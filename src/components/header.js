import { Link } from "react-router-dom";

function Header() {
    return (
        <nav>
            <Link
            to="/">
            Liste des quiz
            </Link>
        </nav>
    )
}

export default Header